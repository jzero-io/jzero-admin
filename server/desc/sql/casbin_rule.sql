CREATE TABLE `casbin_rule` (
    `id` integer AUTO_INCREMENT,
    `ptype` varchar(100),
    `v0` varchar(100),
    `v1` varchar(100),
    `v2` varchar(100),
    `v3` varchar(100),
    `v4` varchar(100),
    `v5` varchar(100),
    PRIMARY KEY (id)
);